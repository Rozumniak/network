
let initialState ={
    friendName:[
        {friendsName: "Diana"},
        {friendsName: "Ihor"},
        {friendsName: "Ruslan"}
    ]
}
const sidebarReducer = (state = initialState, action) => {

    return state;
}
export default sidebarReducer;